<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 读取数据库配置文件 -->
    <context:property-placeholder location="classpath:druid.properties" ignore-unresolvable="true"/>

    <!-- Druid数据源配置 -->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource"
          init-method="init" destroy-method="close">
        <!-- 基本配置 -->
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>

        <!-- 连接池配置 -->
        <property name="initialSize" value="${jdbc.initialSize:5}"/>
        <property name="minIdle" value="${jdbc.minIdle:5}"/>
        <property name="maxActive" value="${jdbc.maxActive:20}"/>
        <property name="maxWait" value="${jdbc.maxWait:60000}"/>
        <property name="timeBetweenEvictionRunsMillis" value="${jdbc.timeBetweenEvictionRunsMillis:60000}"/>
        <property name="minEvictableIdleTimeMillis" value="${jdbc.minEvictableIdleTimeMillis:300000}"/>
        <property name="validationQuery" value="${jdbc.validationQuery:SELECT 1}"/>
        <property name="testWhileIdle" value="${jdbc.testWhileIdle:true}"/>
        <property name="testOnBorrow" value="${jdbc.testOnBorrow:false}"/>
        <property name="testOnReturn" value="${jdbc.testOnReturn:false}"/>
        <property name="poolPreparedStatements" value="${jdbc.poolPreparedStatements:true}"/>
        <property name="maxPoolPreparedStatementPerConnectionSize"
                  value="${jdbc.maxPoolPreparedStatementPerConnectionSize:20}"/>
        <property name="filters" value="${jdbc.filters:stat,wall,log4j}"/>
        <property name="removeAbandoned" value="${jdbc.removeAbandoned:true}"/>
        <property name="removeAbandonedTimeout" value="${jdbc.removeAbandonedTimeout:1800}"/>
        <property name="logAbandoned" value="${jdbc.logAbandoned:true}"/>
    </bean>

    <!-- MyBatis SqlSessionFactory 配置 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- 数据源 -->
        <property name="dataSource" ref="dataSource"/>
        <!-- MyBatis配置文件位置 -->
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <!-- MyBatis映射文件位置 - 使用classpath*:搜索所有路径 -->
        <property name="mapperLocations" value="classpath*:mapper/*.xml"/>
        <!-- 实体类包路径 -->
        <property name="typeAliasesPackage" value="jmu.fyl.logistics.entity"/>
        <!-- 配置属性 -->
        <property name="configurationProperties">
            <props>
                <prop key="mapUnderscoreToCamelCase">true</prop>
                <prop key="logImpl">STDOUT_LOGGING</prop>
            </props>
        </property>
    </bean>

    <!-- MyBatis Mapper扫描器 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="jmu.fyl.logistics.dao"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
        <property name="annotationClass" value="org.springframework.stereotype.Repository"/>
    </bean>

</beans>